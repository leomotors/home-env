# Home Env

There are two components: Client and Server

## Client (Folder: home)

Arduino Code for ESP32

## Server (Folder: server)

Written in Go

Swagger: Use `make docs` to generate swagger.
Format: Use `make format` to format code.

See more command in `Makefile` under `server` folder.
